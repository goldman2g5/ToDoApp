<MudDialog>
    <DialogContent>
        <MudTextField T="string" @ref="NameField" Label="New Board's Name"></MudTextField>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Submit">Ok</MudButton>
    </DialogActions>
</MudDialog>

@code {
    MudForm form;
    MudTextField<string> NameField;
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    void Submit() => MudDialog.Close(DialogResult.Ok(NameField.Value));
    void Cancel() => MudDialog.Cancel();
}